{
  "name": "songbook",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:win": "npm run dev:win --workspace=apps/web",
    "ui-only": "npm run ui-only --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "preview": "npm run preview --workspace=apps/web",
    "deploy": "npm run deploy --workspace=apps/web",
    "check:print-styles": "npm run check:print-styles --workspace=apps/web"
  }
}
